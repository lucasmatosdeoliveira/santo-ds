import { Meta } from "@storybook/blocks";
import { GridsSpec } from "../../components/GridsSpec";

<Meta title="Foundations/Grids" />

# Grids

Sistema de grid responsivo. Três breakpoints: desktop (12 colunas), tablet (8 colunas), mobile (4 colunas).

<GridsSpec />
